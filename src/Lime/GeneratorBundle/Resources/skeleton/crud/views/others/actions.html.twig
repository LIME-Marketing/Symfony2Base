
                <ul class="nav nav-pills">
                {%- for action in record_actions %}
                    <li>
                        <a href="{{ "{{ path('"~ route_name_prefix ~"_"~ action ~"', { 'slug': entity.slug }) }}" }}">{{ action }}</a>
                    </li>
                {%- endfor %}
                </ul>
